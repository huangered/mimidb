#include <cstdlib>
#include <iostream>
#include <fstream>
#include <filesystem>
#include "storage/disk.hpp"

struct A {
    int i;
    uint8_t a[1024];
};
int
main(int argc, char* argv[]) {

    int* p{};
    if (p == nullptr)
    {
        std::cout << "null";
    }
    return 0;
}
